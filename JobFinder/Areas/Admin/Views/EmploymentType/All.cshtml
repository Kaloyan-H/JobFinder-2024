@model IEnumerable<EmploymentTypeServiceModel>

@{
    ViewBag.Title = "Employment Types";
}

<h2 class="text-center">@ViewBag.Title</h2>
<hr />

<div class="container">
    <div class="row">
        <div class="col-md-12">
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">Id</th>
                        <th scope="col">Name</th>
                        <th scope="col"></th>
                        <th scope="col"></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var employmentType in Model)
                    {
                        <tr>
                            <th scope="col">@employmentType.Id</th>
                            <td>@employmentType.Name</td>
                            <td>
                                <form class="text-center" asp-area="Admin" asp-controller="EmploymentType" asp-action="Edit" method="get">
                                    <input type="hidden" name="@nameof(employmentType.Id)" value=@employmentType.Id />
                                    <button class="btn btn-warning" type="submit" role="button">Edit</button>
                                </form>
                            </td>
                            <td>
                                <form class="text-center" asp-area="Admin" asp-controller="EmploymentType" asp-action="Delete" method="get">
                                    <input type="hidden" name="@nameof(employmentType.Id)" value=@employmentType.Id />
                                    <button class="btn btn-danger" type="submit" role="button">Delete</button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
            <form class="text-center" asp-area="Admin" asp-controller="EmploymentType" asp-action="Create" method="get">
                <button class="btn btn-success" type="submit" role="button">Create New Employment Type</button>
            </form>
        </div>
    </div>
</div>
